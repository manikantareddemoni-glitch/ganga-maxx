services:
  - type: web
    name: ganga-maxx-backend
    env: node
    plan: free
    buildCommand: "cd backend && npm install"
    startCommand: "cd backend && npm start"
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: SMTP_USER
        value: manikantareddemoni@gmail.com
      - key: SMTP_PASS
        value: utsnpogbrraqgdby
